using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IconEffect : MonoBehaviour
{
	[SerializeField]
	List<GameObject> lg_Icon = new List<GameObject>();

	[SerializeField]
	GameObject g_Camera;

	[SerializeField]
	GameObject g_Active;
	ActiveManager am_Manager;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
